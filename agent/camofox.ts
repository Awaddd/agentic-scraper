export interface Tabs { create(url: string, sessionKey: string): Promise<Record<string, unknown>>; snapshot(id: string): Promise<Record<string, unknown>>; click(id: string, ref: string): Promise<unknown>; type(id: string, ref: string, text: string, pressEnter?: boolean): Promise<unknown>; scroll(id: string, direction: string): Promise<unknown>; navigate(id: string, url: string): Promise<unknown>; close(id: string): Promise<unknown>; }
export function createCamofox(camofoxUrl: string, userId: string, fetcher: typeof fetch = fetch): Tabs {
  const request = async (method: string, path: string, body?: unknown): Promise<Record<string, unknown>> => { const res = await fetcher(camofoxUrl + path, { method, headers: body ? { "Content-Type": "application/json" } : {}, body: body ? JSON.stringify(body) : undefined }); const text = await res.text(); if (!res.ok) throw new Error(`camo ${method} ${path} -> ${res.status}: ${text.slice(0, 400)}`); return text ? JSON.parse(text) as Record<string, unknown> : {}; };
  return { create: (url, sessionKey) => request("POST", "/tabs", { userId, sessionKey, url }), snapshot: (id) => request("GET", `/tabs/${id}/snapshot?userId=${userId}`), click: (id, ref) => request("POST", `/tabs/${id}/click`, { userId, ref }), type: (id, ref, text, pressEnter = false) => request("POST", `/tabs/${id}/type`, { userId, ref, text, pressEnter }), scroll: (id, direction) => request("POST", `/tabs/${id}/scroll`, { userId, direction }), navigate: (id, url) => request("POST", `/tabs/${id}/navigate`, { userId, url }), close: (id) => request("DELETE", `/tabs/${id}?userId=${userId}`) };
}
export async function wakeBrowser(camofoxUrl: string, fetcher: typeof fetch = fetch): Promise<void> { try { await fetcher(`${camofoxUrl}/health`); } catch {} }
export async function createTabWithRetry<T>(createTab: (url: string, sessionKey: string) => Promise<T>, url: string, sessionKey: string, wake: () => Promise<void>): Promise<T> { let lastError: unknown; for (let attempt = 1; attempt <= 3; attempt++) { try { return await createTab(url, sessionKey); } catch (error) { lastError = error; const message = error instanceof Error ? error.message : String(error); if (!message.includes("503") && !message.includes("session_expired")) throw error; if (attempt < 3) await wake(); } } throw lastError; }
