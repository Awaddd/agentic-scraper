import { describe, expect, it, vi } from "vitest";
import { createHmac } from "node:crypto";
import { buildWebhookHeaders, buildWebhookPayload, deliverWebhook } from "./webhook.js";
describe("webhook", () => { it("preserves exact payload bytes and signs those bytes", async () => { const payload = buildWebhookPayload({ jobId: "a", type: "jobs", ok: true, result: [], tokens: { prompt: 1, completion: 2, total: 3 }, steps: 1, durationMs: 4 }); const body = JSON.stringify(payload); const headers = buildWebhookHeaders(body, "secret"); expect(headers["X-Scraper-Signature"]).toBe(createHmac("sha256", "secret").update(body).digest("hex")); const fetcher = vi.fn().mockResolvedValue(new Response("", { status: 200 })); await deliverWebhook("https://hook.test", payload, "secret", fetcher); expect(fetcher.mock.calls[0]?.[1]?.body).toBe(body); }); it("does not add optional signature headers without a secret", () => expect(buildWebhookHeaders("{}", undefined)).toEqual({ "Content-Type": "application/json" })); });
